const events = require('./events');
const interactive = require('./interactive');
const slash = require('./slash');
const system = require('./system');

module.exports = { events, interactive, slash, system };
